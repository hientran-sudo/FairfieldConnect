@model FindPost
@{
    ViewData["Title"] = "Delete";
}

<h3>Delete</h3>
<h4>Are you sure you want to delete this finding?</h4>
<div>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Title)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Desc)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Desc)
        </dd>
        
    </dl>
    <form asp-action="DeleteFinding" method="post">
        <input type="hidden" asp-for="FindPostID" />
        <button type="submit" class="btn btn-danger"> Delete </button>
        <button asp-controller="Housing" asp-action="Find" class="btn btn-warning">Cancel</button>
    </form>
</div>
